import template from './bookDetail.html';
import controller from './bookDetail.controller';

let bookDetailComponent = {
    restrict: 'E',
    bindings: {},
    template,
    controller
};

export default bookDetailComponent;
